
project.bin = ../bin
project.src = ../src

<!-- Need a better name for this -->
project.packaging.jar = project-beans.jar
project.servlets      = project-servlets.war

project.persistence.sar = persistence.sar

mbeans.crawler.sar  = crawler.sar
mbeans.analysis.sar = analysis.sar
mbeans.manager.sar  = connection-manager.sar
mbeans.processing.sar = message-processing.sar



application.dir = bachelor-project

<!-- Folder where JBoss is installed -->
server.jboss.directory=/opt/java/jboss

<!-- JBoss configuration (minimal, default, full) -->
server.jboss.configuration=default

<!-- Complete path for cp -->
server.cp          =${server.jboss.directory}/server/${server.jboss.configuration}/deploy